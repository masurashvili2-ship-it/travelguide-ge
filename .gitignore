# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# Real accounts — do not commit (copy data/users.json.example → data/users.json locally).
# On App Platform use a persistent volume on `data/` so registrations survive redeploys.
data/users.json
# local sqlite (if used)
*.db
# optional: ignore tour data (remove line to commit content)
# data/tours.json

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
