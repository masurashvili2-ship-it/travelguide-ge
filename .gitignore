# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# Keep data/users.json in git so production has the store (see data/USERS-DEPLOY.md).
# Use a persistent disk on App Platform so new registrations survive redeploys.
# local sqlite (if used)
*.db
# optional: ignore tour data (remove line to commit content)
# data/tours.json

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
